import { LightningElement,api } from 'lwc';

export default class Parent extends LightningElement 
{
    @api parentX;
//     parentHandler(event)
//     {
//      this.parentX = event.detail.message;
//      this.dispatchEvent(new CustomEvent('notification', {
//         detail: {
//             message:this.parentX
           
//         }
//     }));
     
//    }
}